changes:
- type: feature
  component: general
  description: add `GithubClientSettings` class and accept a settings parameter in
    `GithubApp.installation_client()` and the new `GithubApp.app_client()` method
